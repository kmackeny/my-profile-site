name: Secret Test

on:
  workflow_dispatch:

jobs:
  secret-test:
    runs-on: ubuntu-latest
    steps:
      - name: Secret を使う
        env:
          MY_API_KEY: ${{ secrets.MY_API_KEY }}
        run: |
          echo "Secret is set: ${MY_API_KEY:+yes}"
